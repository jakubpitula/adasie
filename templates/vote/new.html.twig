{% extends 'base.html.twig' %}
{% block title %}Vote{% endblock %}

{% block body %}
    {# {{dump(form.minivotes)}} #}

    <p><a href="{{ path('teacher_index') }}">Teacher List</a></p>
    <p><a href="{{ path('category_index') }}">Category List</a></p>

    {{ form_start(form) }}

    <div class="row d-flex justify-content-center">
    {% for minivote in form.minivotes %}
        <div class="col-xs-4">
            {{ form_row(minivote, {'label': minivote.vars.value.category.name}) }}
        </div>
    {% endfor %}
    </div>
    <div class="d-flex justify-content-center">
        {{form_row(form.submit, {'label': 'Zagłosuj!'})}}
    </div>
{{ form_end(form) }}
{% endblock %}
