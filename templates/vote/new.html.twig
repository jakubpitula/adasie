{% extends 'base.html.twig' %}
{% form_theme form 'themes/layout.html.twig' %}

{% block title %}Adasie 2019{% endblock %}

{% block body %}
    <div class="row d-flex justify-content-center mb-5 ml-3 mr-3 mt-5">
    <h1 class="display-2"><em>ADASIE 2019</em></h1>
    </div>

    <div class="row d-flex justify-content-center mb-5 ml-3 mr-3 mt-3">
        <div class="w-75 alert alert-warning h2" style="background-color: #dddddd; color: black;">
            <center>
                Witaj w głosowaniu!<br>
                W każdej kategorii wybierz, z ułożonej alfabetycznie według nazwisk listy,
                nauczyciela, który, Twoim zdaniem, najlepiej do niej pasuje.<br>
                Jeśli nie masz pomysłu, pozostaw opcję <strong>brak</strong>.<br>
                Głosy zatwierdź przyciskiem <strong>ZAGŁOSUJ</strong> na dole strony.<br>
                Cieplutkie pozdrowionka od Szkolnej Rady Uczniowskiej.
            </center>
        </div>
    </div>
    {{ form_start(form) }}
    <div class="container">
        {% for minivote in form.minivotes %}
            <div class="row d-flex justify-content-center">
                {{form_label(form, minivote.vars.value.category.name, {
                    'label_attr': {'class': 'col-sm-10 text-center', 'style': 'font-size: 70px !important; font-weight: bold;'}
                })}}
            </div>
            <div class="col">
            {{ form_widget(minivote, {
                'attr': {'class': 'd-flex justify-content-center p-1 m-4'}
            }) }}
            </div>
        {% endfor %}
        <div class="d-flex justify-content-center">
            {{form_row(form.submit, {
                'label': 'ZAGŁOSUJ',
                'attr': {'style': 'font-size: 40px !important; padding: 15px; background-color: #3e666d; border: none; font-weight: bold;'}
            })}}
        </div>
        <div class="d-flex justify-content-center">
            {{form_errors(form)}}
        </div>
        {% if voted is defined %}
            <div class="d-flex justify-content-center" style='font-size: 40px !important;'>
                {# <span class="alert alert-danger d-block" style="background-color: black !important; color: white !important;"> #}
                    <span class="d-block">
                        <span class="form-error-icon badge badge-danger text-uppercase">Błąd</span> 
                        <span class="form-error-message" style="color:black;">Można zagłosować tylko raz.</span>
                    </span>
                {# </span> #}
            </div>
        {% endif %}
    </div>
{{ form_end(form) }}
{% endblock %}
