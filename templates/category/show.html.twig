{% extends 'base.html.twig' %}

{% block title %}Kategoria{% endblock %}

{% block body %}
    <h1>Kategoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">Powrót do listy</a>

    <a href="{{ path('category_edit', {'id': category.id}) }}">edytuj</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
